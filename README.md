# Javanary
### A FOSS dictionary application.


I'll make a release for all OS's. 

The releases are portable, no JDK needed.

Do whatever with the src code.

The dir 'DictionaryData' has all the txt files but they're all empty and missing the way you're suppose to format(ill leave the format down below). You're gonna have to add your own definitions. Would like help with that. 

When you run it, it will create a dir and txt files.

*Try to figure what my code does. I honestly don't remember why I wrote it the way I did, It comes to me in a vision then god takes over and writes the solution.*

Section file format:
[word]{speech}"definition""

Or consult 'a.txt' or 'c.txt', they are the only files with examples. 

Just like that, no spaces. I didn't write any handling in case you didn't correctly write the format out. 
A def crash will occur lol.